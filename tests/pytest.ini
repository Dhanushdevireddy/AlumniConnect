[pytest]
testpaths = unit integration
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit-level UI tests (no actual API interaction needed)
    integration: End-to-end integration tests (require live frontend + backend)
addopts = -v --tb=short
